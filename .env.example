# OPTIONAL: These values are only used during initial database seeding (db:seed)
# After the database is set up, these settings are managed via the admin UI at /app_configs
# You can leave these blank and configure them through the web interface instead.

# SMTP Email Settings (optional - configure via admin UI)
SMTP_ADDRESS=
SMTP_PORT=
SMTP_DOMAIN=
SMTP_USER_NAME=
SMTP_PASSWORD=

# PayPal Settings (optional - configure via admin UI)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=

# Google API Settings (optional - configure via admin UI)
GOOGLE_WEB_CLIENT_BASE64=
GOOGLE_MEMBERS_GROUP=
GOOGLE_REMOVE_GROUP=
SJAA_GOOGLE_API_KEY=
SJAA_ALL_EVENTS_CALENDAR_ID=
SJAA_MERGED_CALENDAR_ID=

# REQUIRED: Deployment and infrastructure settings
# These must be set as environment variables and cannot be configured via the admin UI

# For pulling sjaa packages. Needs a token with read:packages scope.
GITHUB_USERNAME=
GITHUB_TOKEN=